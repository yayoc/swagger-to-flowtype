openapi: "3.0.1"
info:
  title: Hello, World!

paths:
  /hello:
    get:
      responses:
        200:
          content:
            application/json:
              schema:
                title: HelloWorldResponse
                type: object
